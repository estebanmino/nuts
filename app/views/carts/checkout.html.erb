<% if !logged_in? %>

      <h3>Ingrese a su cuenta</h3>

      <%= render 'sessions/checkout_form' %>

      <h3>o continue como invitado</h3>

      <div class="row">
        <div class="col-md-6 col-md-offset-3 col-lg-6 col-lg-offset-3">
          <br>
          <div class="center"><%= button_to 'Continuar como invitado', shipping_path, class: "btn btn-primary", method: :get %></div>
        </div>
    
  </div>
<% else %>
  <% controller.redirect_to confirmation_path %>
<% end %>
<br>
<br>
<br>
