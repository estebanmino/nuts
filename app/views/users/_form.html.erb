<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <%= form_for(@user, url: signup_path) do |f| %>
      <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <%= render 'shared/error_messages' %>

      <h3><%= f.label :name, "Nombre completo" %></h3>
      <%= f.text_field :name, class: "form-control"  %>

      <h3><%= f.label :email, "Correo electrónico" %></h3>
      <%= f.email_field :email, class: "form-control" %>

      <h3><%= f.label :address, "Dirección de despacho" %></h3>
      <%= f.text_field :address, class: "form-control" %>

      <h3><%= f.label :password, "Contraseña" %></h3>
      <%= f.password_field :password, class: "form-control" %>

      <h3><%= f.label :password_confirmation, "Confirme contraseña" %></h3>
      <%= f.password_field :password_confirmation, class: "form-control" %>
      <br>

      <%= f.submit "Crear cuenta", class: "btn btn-primary form-control" %>

    <% end %>

  </div>
</div>
